# 兼容性矩阵

[简体中文](COMPATIBILITY.md) · [繁體中文](../Zh-tw/COMPATIBILITY.md) · [English](../en/COMPATIBILITY.md) · [返回简中 README](README.md)

本文只记录实际完成的验证，不把“能够编译”写成“已经兼容”。

## 开发中的目标

| Minecraft | 加载器 | Java | 构建与自测 | 实际启动到主菜单 | 服务器内完整人工回归 |
| --- | --- | --- | --- | --- | --- |
| 26.1 | Fabric Loader 0.19.3 + Fabric API 0.145.1 | 25 | 通过 | 通过 | 待验证 |
| 26.2 | Fabric Loader 0.19.3 + Fabric API 0.157.0 | 25 | 通过 | 待验证 | 待验证 |
| 26.3 | Fabric Loader 0.19.5 + Fabric API 0.161.0 | 25 | 通过 | 待验证 | 待验证 |
| 1.21.10 | Fabric Loader 0.18.1 + Fabric API 0.138.4 | 21 | 通过 | 待验证 | 待验证 |
| 1.18.1 | Fabric Loader 0.16.10 + Fabric API 0.46.6+1.18 | 17 | 通过 | 待验证 | 待验证 |
| 1.16.5 | Fabric Loader 0.16.10 + Fabric API 0.42.0+1.16 | 8 | 通过 | 通过 | 待验证 |

1.16.5 使用旧版 Loom 与单一 `src/main` 源集（该版本没有独立服务端 JAR），Yarn 映射为
`1.16.5+build.10`；界面按该版本 API 改用 `MatrixStack`、`Screen#onClose`/`isPauseScreen`、
`TextFieldWidget` 与 `ButtonWidget.TooltipSupplier`。已完成干净构建、重混淆、共享核心自测，
并实际启动到主菜单，Mixin 注入日志无失败项；受 1.16.5 API 限制，该版本不提供全息文本
（TextDisplay）翻译，且只有 Fabric 端，没有对应 Forge/NeoForge 工程。
1.21.10 已按 1.21.11 的 49 个源文件完成完整 Fabric 功能适配，使用 Yarn
`1.21.10+build.3` 和 Fabric API `0.138.4+1.21.10`；本机已完成编译与重混淆，实机启动和服务器内回归仍待验证。
1.18.1 与 1.17.1 一样属于旧版 Loom 与单一 `src/main` 源集，使用 Yarn `1.18.1+build.22`
和 Fabric API `0.46.6+1.18`；已完成干净构建、重混淆、共享核心自测，并用映射后的
1.18.1 类文件逐个核对了全部 16 个 Mixin 的注入目标和 `@Shadow` 字段。该版本同样只有
Fabric 端，且因 1.18 没有 TextDisplay API 而不提供全息文本翻译，实机启动和服务器内
回归仍待验证。
26.1 已完成干净构建、Mixin 注入检查和实际启动到主菜单。26.2 已迁移至新的
`Minecraft.gui` 与 `Hud` API，并核对聊天、记分板、Tab、标题、Action Bar、
Boss Bar、文字绘制和 tooltip 的 Mixin 目标；目前只记录干净构建、重混淆和共享
核心自测。26.3 在 26.2 基础上适配移除 GLFW 后的输入常量和告示牌消息列表返回
类型变化，已完成干净构建、重混淆、全部 Mixin 目标类核对和共享核心自测；该版本
目前只有 Fabric 端，没有对应的 Forge/NeoForge 工程。这些版本都需完成服务器内
人工回归后才会进入正式发布表。

## 1.1 发布验证

1.1 对三个目标版本执行干净构建、重混淆和共享核心自测。此次更新集中于
物品名称与 Lore 的成组翻译、下载进度显示、失败提示和 API 回退状态；
服务器内人工兼容性仍以 1.0 的实机启动结果为基线，不把本次自动构建写成新的实机验证。

## 1.0 已验证目标

| Minecraft | 加载器 | Java | 构建与自测 | 实际启动到主菜单 | 服务器内完整人工回归 |
| --- | --- | --- | --- | --- | --- |
| 1.8.9 | Forge 11.15.1.2318 | 8 | 通过 | 通过 | 待下一轮真人操作确认 |
| 1.12.2 | Forge 14.23.5.2864 | 8 | 通过 | 通过 | 待下一轮真人操作确认 |
| 1.21.11 | Fabric Loader 0.19.3 + Fabric API 0.141.6 | 21 | 通过 | 通过 | 待下一轮真人操作确认 |

三种构建共享同一套翻译核心。真实 Qwen2.5 0.5B 离线模型探针已验证英文翻译、
中英文混合分段、玩家名、服务器地址和数字的本机保护与重组。1.0 还确认
Forge 两版本的通用提示与物品生成入口两个 tooltip 钩子均已安装，Fabric 1.21.11
的标准物品 tooltip Mixin 在翻译开启时无注入错误；
物品名与 Lore 的最终视觉效果仍列入服务器内人工回归。

## “支持所有版本”的原则

Minecraft 各时代的加载器、Java 版本、渲染 API 和文字组件结构不同，无法用一个
JAR 安全覆盖所有 Java 版。项目会按有代表性的长期使用版本逐条适配，并为每条发布线
保留独立 JAR。只有完成构建、启动、服务器内界面回归后，才会进入“已验证”表。

后续候选顺序：

1. 1.12.2 Cleanroom；
2. 1.7.10 Forge；
3. 1.16.5 Fabric 已完成适配（见上表），Forge 端仍待评估；
4. 1.18.2 Forge/Fabric；
5. 1.20.1 Forge/Fabric；
6. 1.21.1 Fabric/NeoForge；
7. 后续仍有较多玩家使用的新版本。

在 1.8.9、1.12.2、1.16.5、1.21.10、1.21.11、26.1、26.2 和 26.3 的服务器内回归全部通过前，不开始扩展上述版本，
以免同时引入更多未经验证的渲染差异。

## 服务器内回归清单

- 聊天、记分板、Tab 列表、Action Bar、标题和 Boss Bar；
- 箱子/菜单标题、物品名与 Lore、告示牌、书、实体名和全息文字；
- 玩家自己输入和发出的聊天保持原文；
- 在线玩家名称、本机名称、服务器 IP/域名/端口保持原样；
- 已有中文不重复翻译，英文片段可翻译；
- 仅译文模式无双语溢出，颜色选项生效；
- `U` 控制面板和 `F8` 总开关在不同窗口尺寸下可用；
- 模型下载中、下载失败、离线引擎失败时游戏不冻结，继续显示原文。
